import { ReplaceBatonPipe } from "./replace-baton.pipe";

describe("ReplaceBatonPipe", () => {
	it("create an instance", () => {
		const pipe = new ReplaceBatonPipe();
		expect(pipe).toBeTruthy();
	});
});
